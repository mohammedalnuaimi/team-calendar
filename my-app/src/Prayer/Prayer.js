import React from 'react'
const Prayer = (props) => {
        return (
            <button className="prayer" onClick={function () { alert('YOU CLICKED ME!'); }}>
                
            </button>
        );
}

export default Prayer;